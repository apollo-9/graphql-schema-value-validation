{
  "name": "graphql-schema-value-validation",
  "version": "0.0.1",
  "description": "Exposes custom directives for doing validation in your schema",
  "main": "index.js",
  "scripts": {
    "format": "npm run prettier --write \"**/*.js\"",
    "prettier": "prettier \"**/*.js\"",
    "lint": "eslint .",
    "validate": "npm run lint && npm run prettier -- --list-different",
    "precommit": "lint-staged",
    "build": "npm run validate"
  },
  "repository": {
    "url": "https://github.com/apollo-9/graphql-schema-value-validation"
  },
  "author": "Apollo9",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.10.0",
    "husky": "^1.2.0",
    "lint-staged": "^8.1.0",
    "prettier": "^1.15.3"
  },
  "dependencies": {
    "eslint-config-prettier": "^3.3.0"
  }
}
